{
"name": "tms",
"private": true,
"scripts": {"setup": "npm install && npm install --prefix frontend && npm install --prefix backend",
"prepare": "husky",
"lint": "npm run lint --prefix frontend && npm run lint --prefix backend"
},
"devDependencies": {
"husky": "^9.1.7",
"lint-staged": "^17.4.1"
}
}